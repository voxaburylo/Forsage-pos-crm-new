services:
  - type: web
    name: forsage-api
    runtime: node
    buildCommand: npx pnpm install --no-frozen-lockfile && npx pnpm --filter server build
    startCommand: node server/dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
    autoDeploy: true
