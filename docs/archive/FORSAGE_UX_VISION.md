# Форсаж CRM/POS — Product Vision & UX/UI Overhaul Plan

> Документ-стратегія. Складено на основі **реального коду** системи (не загальні поради):
> реальні екрани, компоненти й файли названо прямо, щоб план був одразу впроваджуваним.
> Стек: React 18 + TS + Vite + Zustand + Tailwind 4 / Express / Supabase (PWA, офлайн-каса).
> Бренд: `#FFD000`. Гроші — копійки (integer). Інтерфейс — українською. Ролі: owner/admin/manager/cashier/storekeeper.

---

## 0. Чесна оцінка поточного стану

**Що вже сильно (не ламати):**
- Уніфікована прийомка в `InvoiceFormPage` — імпорт Excel/CSV/буфер з авто-мапінгом колонок, фото в рядку, генерація штрихкодів, сітка націнок + швидкі %, порівняння цін постачальників, оплата + борги.
- POS: окремі панелі (`SearchPanel` F4 + trgm-пошук, `ReceiptPanel`, `FavoritesPanel`, `CrossSellPanel`, `ReadyOrdersPanel`), гарячі клавіші F1–F8, темна тема, офлайн-PWA, ідемпотентність оплат.
- Drawer «Швидкий перегляд» у `OrdersPage`, інлайн-редагування ціни в `ProductsPage`, злиття дублів, борги/комісії.

**Кореневі UX-болі (їх і лікуємо):**
1. **«Модалка-водоспад».** Є лише `Modal` (центрована, перекриває весь контекст). Складні дії (картка товару, налаштування знижки, оплата) тиснуться в центр екрана — користувач втрачає список, з якого прийшов.
2. **Багатокрокові переходи замість «одного вікна».** Класичний приклад: створити накладну (чернетку) → піти в список → відкрити → **«Провести»**. Дія розірвана на 2 екрани.
3. **Ряди з 3+ кнопками** (`ProductsPage`: Ред / Злити / Видал; дії позицій замовлення) — візуальний шум і промахи пальцем.
4. **Немає системних примітивів:** немає переіспользовуваного `Drawer`, `Skeleton`, `Tabs`, `Dropdown/SplitButton`, `Tooltip`. Кожен екран винаходить їх заново → неконсистентність.
5. **Стани завантаження = текст «Завантаження…»** замість кістякових екранів → відчуття «гальмує».

---

## 1. Аудит користувацьких шляхів (з підрахунком кліків)

### 1.1. Прийом великої партії від НОВОГО постачальника
**Зараз (≈ 12–15 кліків + перехід між екранами):**
`Постачальники → Прихідні накладні → Нова` → «+» біля постачальника → модалка (назва, телефон, Зберегти) → вкладка «Імпорт Excel/CSV» → завантажити файл → перевірити мапінг → «Роздрібні за націнкою» → пройтись по рядках (фото/штрихкод/комірка) → блок оплати → **Створити** → *(окремо)* список → відкрити накладну → **Провести**.

**Вузькі місця:** (а) створення/проведення розірване; (б) постачальник — окрема модалка поверх; (в) немає прогресу для великих файлів (1000+ рядків імпортуються в браузері).

**Концепція «одне вікно» — Майстер прийомки (single-screen wizard):**
- Один екран `/suppliers/invoices/new` із **горизонтальним степером** угорі: `1 Постачальник · 2 Товари · 3 Ціни · 4 Оплата та проведення`. Не нові сторінки — секції на одному екрані зі «sticky»-підсумком справа (сума, к-сть позицій, борг).
- Постачальник: **інлайн-комбобокс «створи-на-льоту»** (як категорія/бренд у `ProductFormPage`), без модалки. Ввів назву якої нема → Enter → створено й вибрано.
- Чекбокс **«Провести одразу після створення»** (за замовч. увімкнено) — прибирає окремий крок «Провести». Чернетка лишається опцією.
- Імпорт 1000+ рядків → **серверний воркер** (інфраструктура `jobs`/BullMQ вже є) з прогрес-баром і toast «Готово», замість зависання вкладки.
- **Ефект:** ~12 кліків → ~5, нуль переходів між сторінками.

### 1.2. Швидкий продаж клієнту з персональною знижкою (СТО)
**Зараз:** POS → (відкрити зміну) → F4 пошук → додати → **«+ Клієнт»** (окрема панель/пошук) → знижка цінового рівня застосовується → F2 оплата → метод → готово.

**Вузькі місця:** вибір клієнта — окремий крок; неочевидно, **чому** ціна змінилась (застосувався tier СТО); знижка «на чек» vs «персональна» плутаються.

**Концепція безшовності:**
- Поле клієнта **прямо в шапці чека** (`ReceiptPanel`) з інлайн-пошуком по телефону/імені/картці — без переходу. Сканер картки клієнта (штрихкод) одразу підставляє його.
- Коли вибрано СТО — **бейдж біля підсумку**: «Ціни СТО −10%» з підказкою, які рядки змінились (прозорість знижки).
- **Split Button «Оплатити (F2)»**: основна дія — повна оплата готівкою; випадне меню — «Картка / Змішано / Борг / Передоплата». Один елемент замість 4 кнопок.
- **Ефект:** продаж із персональною ціною — у межах однієї панелі, без перекриття контексту.

### 1.3. Менеджер зі збірки замовлень (WMS)
**Зараз (вже непогано):** `Склад та Збірка` → список → відкрити → сканувати/відмічати позиції → ввести комірку → готово. Додано банер «Як це працює» та виправлено лічильники вкладок.

**Що покращити:**
- **Режим «сканер-фокус»**: великий рядок поточної позиції + звук/вібро на збіг (звуки вже є — `audioService`), решта списку приглушена. Комірник не клікає — лише сканує.
- **Прогрес-бар зборки** («5 з 8 зібрано») і авто-перехід до наступної непідібраної позиції.
- Сортування за коміркою вже є (natural sort) — додати **«маршрут складом»** (порядок обходу).

---

## 2. Сучасна концепція UX/UI

### 2.1. Дизайн-система «Forsage Industrial» (преміально, але швидко)
Концепт: **«інструмент майстра»** — жовтий бренд `#FFD000`, графіт, висока щільність даних, без зайвого «повітря» (це робоча система на 8 годин/день, не лендінг).

**Палітра (токени в `index.css`, розширити):**
| Роль | Light (адмін) | POS (темна) |
|---|---|---|
| Акцент | `#FFD000` (CTA, активні) | `#FFD000` |
| Фон | `#F5F5F5` | `#1A1A1A` |
| Поверхня/картка | `#FFFFFF` | `#2C2C2C` |
| Текст основний | `#111827` | `#F5F5F5` |
| Успіх / Борг / Увага | `#10B981` / `#EF4444` / `#F59E0B` | ті самі, трохи яскравіші |
| Бордер | `#E5E7EB` | `#3A3A3A` |
Додати **семантичні токени** (`--surface`, `--text-muted`, `--border`, `--danger`) і перейти на них замість хардкоду `gray-100` по компонентах → одна точка правди, готовність до темної теми всюди.

**Типографіка:** залишити system-font стек (швидко, рідно), але **сітка розмірів**: 11/12 (мета-дані, мобільні картки) · 13/14 (основний) · 16 (заголовки секцій) · 20/24 (суми, KPI). Числа/гроші — `font-variant-numeric: tabular-nums` (рівні колонки в таблицях).

**Сітка відступів:** база 4px (вже de-facto). Картки `rounded-2xl`, кнопки `rounded-xl`, інпути `rounded-lg`.

### 2.2. Як зробити «живим» і преміальним
- **Skeleton loaders замість «Завантаження…».** Новий `components/ui/Skeleton.tsx` + `TableSkeleton`. `Table` і списки показують 5–8 «привидів» рядків. Психологічно швидше навіть за тієї ж швидкості.
- **Glassmorphism — точково, не всюди.** Шапка модалки/drawer і sticky-підсумок чека: напівпрозорий фон `bg-white/80 backdrop-blur-md` з тонким бордером. На темній POS — `bg-[#2C2C2C]/70 backdrop-blur`. (Не зловживати: дані-таблиці — щільний непрозорий фон для читабельності.)
- **Мікро-анімації (швидкі, 120–200ms):** `slide-up` модалки й `slide-left` drawer вже є. Додати: підсвітку рядка після інлайн-збереження (короткий жовтий flash), `scale-[0.98]` на натиск (вже місцями є), плавний counter для сум.
- **Hover-стани:** у таблицях — підсвітка рядка + поява дій (зараз дії завжди видимі → шум). На hover показувати Split-кнопку дій.
- **Toast** уже є — додати «undo» (Скасувати) на 5с для деструктивних дій (видалення товару, скасування позиції) → менше confirm-модалок.

### 2.3. Навігація: вмістити все без перевантаження
Поточний `Sidebar` (4 групи) — **чистий, зберегти структуру.** Покращення:
- **Глобальний Command Palette (Ctrl/⌘+K)** — пошук по всьому: товар, клієнт, замовлення, дія («створити накладну», «відкрити зміну»). Для досвідченого користувача — найшвидший шлях, прибирає 80% навігації мишею.
- **Під-вкладки (`SubNavTabs`)** винести в окремий `Tabs`-примітив (зараз логіка підсвітки дублюється в `SubNavTabs` і `Sidebar`). Перевантажені групи (Склад — 7 вкладок) — згрупувати: основні вкладки + «Ще ▾» (overflow-меню) для рідкісних (Резерви, Цілісність).
- **Хлібні крихти** на глибоких екранах (картка товару, накладна) — швидкий шлях назад без втрати контексту.

### 2.4. Модальні вікна → принцип «Drawer-first»
**Головна архітектурна заміна.** Правило:
- **Швидке редагування/перегляд у контексті списку → `Drawer` (бічна панель справа).** Користувач бачить список ліворуч, редагує праворуч. Вже доведено патерном «Швидкий перегляд» у `OrdersPage` — **витягнути в `components/ui/Drawer.tsx`** (анімація `slide-left` вже є) і застосувати: картка товару, картка клієнта, налаштування знижки, деталі накладної.
- **`Modal` лишається** тільки для коротких рішень-перепонів: підтвердження, оплата, вибір з 2–3 опцій.
- **Upgrade самого `Modal`:** glass-шапка `bg-white/80 backdrop-blur`, sticky-футер з діями (зараз дії всередині скролу — губляться), `max-h` із внутрішнім скролом тіла (вже є). Розмір `xl` для майстрів.
- **Split Button** (новий `components/ui/SplitButton.tsx`): головна дія + ▾ для варіантів. Замінює «грона» кнопок: оплата в POS, дії рядка товару (Ред ▾ → Злити/Видалити/Друк етикетки).

---

## 3. Оптимізація робочих просторів

### 3.1. Ідеальний дашборд касира (швидкість понад усе)
- **Розкладка 3 зони:** ліворуч — пошук+результати (`SearchPanel`), центр — швидкі кнопки/категорії (`FavoritesPanel`), праворуч — чек (`ReceiptPanel`) зі sticky-підсумком і Split «Оплатити (F2)».
- **Сканер — головний інструмент:** глобальний фокус на штрихкод (працює навіть коли курсор не в полі), звук на збіг, авто-додавання. Невідомий штрихкод → інлайн «Створити товар» прямо в чеку.
- **Гарячі клавіші — видимі:** F1 Довідка, F2 Оплата, F3 Вкладка, F4 Пошук, F5 Відкладені, F8 Сканер — підказки під рукою (вже частково є), + «cheat sheet» по F1.
- **Кілька чеків (вкладки)** — вже є; додати індикатор суми на кожній вкладці.
- **Стан зміни завжди видно**: відкрита/закрита, готівка в касі, № зміни — у шапці.
- **Нуль модалок на гарячому шляху:** клієнт, знижка, оплата — інлайн/drawer, щоб касир не втрачав чек з очей.

### 3.2. Ідеальний інтерфейс адміністратора/закупника (робота з таблицями)
- **Таблиця товарів — як «Excel у браузері»:** інлайн-редагування не лише ціни (вже додано), а й **залишку, комірки, мін. залишку** прямо в комірці (клік → інпут → Enter). Tab/Enter — перехід між клітинками.
- **Масові дії** (`BulkEditModal` вже є) → підсилити: вибрав чекбоксами → плаваюча панель дій унизу («Змінити націнку на %», «Призначити категорію», «Друк етикеток», «Експорт»).
- **Drag-and-drop:** фото товару — перетягнути файл прямо на рядок (зараз клік-аплоад є, додати DnD-зону); накладна — перетягнути Excel-файл на сторінку імпорту (drop-zone).
- **Збережені фільтри/представлення:** «Мало товару», «Без фото», «Без штрихкоду», «Без категорії» — швидкі чіпи над таблицею (частина вже є як фільтри).
- **Колонки під себе:** показати/сховати колонки, запам'ятати в localStorage.

---

## 4. Інноваційні Smart-фічі (5 шт.)

1. **Розумне ціноутворення з підказкою маржі.** Під час прийомки, коли вводиш закупівлю, система показує: рекомендовану роздрібну за сіткою націнок (вже є) **+ підказку**: «У конкурента/у вашому минулому продажу було X», «Маржа Y%», попередження «нижче собівартості». Дані вже частково є (`getSupplierPrices`, історія цін).
2. **Прогноз залишків і авто-список закупівлі.** Замість «Автозакупок» (прибрали як складний рушій) — простий **«Що закупити»**: на основі швидкості продажу (sales velocity) і точки замовлення показує «закінчиться через N днів», «замовити X». Власник підтверджує одним кліком → авто-PO постачальнику (інфраструктура `supplier_purchase_orders` вже є).
3. **Авторозпізнавання накладної (OCR/AI).** Фото/PDF паперової накладної → витягти рядки (артикул, к-сть, ціна) → у форму прийомки. OCR-інфраструктура вже частково є (`ocr_photo` job, Google Vision). Це вбиває ручне введення великих накладних.
4. **AI-підбір аналогів і крос-сел на касі.** Уже є аналоги по тірах брендів і `CrossSellPanel`. Додати: «з цим товаром часто беруть…» (cobuy-дані вже є `getCobuy`) + «немає в наявності → ось аналог у наявності» прямо в пошуку.
5. **Розумні сповіщення власнику в Telegram (бот вже є).** Ранковий дайджест: вчора виторг/маржа/каса/борги/завислі замовлення — одним повідомленням. Аномалії: «продажів сьогодні −40% проти середи», «5 товарів пішли в мінус». Власник «тримає руку на пульсі» без відкриття CRM.

---

## 5. Roadmap впровадження

### Quick Wins (1–3 дні, вау-ефект, низький ризик)
- `components/ui/Drawer.tsx` — витягти з `OrdersPage`; перевести картку товару й клієнта на Drawer.
- `components/ui/Skeleton.tsx` + `TableSkeleton` — замінити всі «Завантаження…».
- `SplitButton` — застосувати в POS (оплата) і рядку товару (дії).
- Інлайн-редагування залишку/комірки в таблиці товарів (за зразком ціни).
- Чекбокс «Провести одразу» в накладній (прибрати окремий крок).
- Toast «Скасувати» (undo) для видалень → менше confirm-модалок.
- Tabular-nums для всіх грошових колонок.

### Mid-term (структурні зміни UX, 1–3 тижні)
- **Майстер прийомки** на одному екрані (степер + sticky-підсумок + інлайн-постачальник).
- **Command Palette (⌘K)** — глобальний пошук і дії.
- Семантичні токени дизайну + єдиний `Tabs`-примітив; згрупувати перевантажені під-вкладки (Склад) у «Ще ▾».
- POS: клієнт і знижка інлайн у `ReceiptPanel`, прозорість цінового рівня.
- Серверний імпорт-воркер прайсів з прогрес-баром.
- Drag-and-drop фото на рядок; drop-zone для Excel.

### Long-term (архітектура + супер-фічі, 1–3 міс.)
- Дизайн-система як пакет (`@forsage/ui`) з повною темною темою скрізь.
- Прогноз залишків + «Що закупити» (sales velocity).
- OCR-прийомка накладних (AI).
- Аналітичний «пульс» бізнесу + Telegram-дайджест/аномалії.
- (Стратегічно, окремо) Мультимагазин/SaaS — найбільший важіль, але потребує переробки tenant і білінгу (див. `FINAL_PRODUCT_AUDIT.md`).

---

## Додаток A: конкретні заміни «до → після»

| Зараз | Стане | Файл |
|---|---|---|
| 3 кнопки в рядку (Ред/Злити/Видал) | **Split Button** «Ред ▾» | `ProductsPage.tsx` |
| Картка товару = окрема сторінка `/products/:id` | **Drawer** праворуч поверх списку | `ProductsPage` + `Drawer.tsx` |
| Створити накладну → список → Провести | Чекбокс **«Провести одразу»** | `InvoiceFormPage.tsx` |
| `Modal` для перегляду замовлення/товару | **Drawer** (контекст списку видно) | `OrdersPage` патерн → `ui/Drawer` |
| «Завантаження…» текст | **Skeleton** рядки | `Table.tsx` + `Skeleton.tsx` |
| Постачальник через «+» модалку | Інлайн комбобокс «створи-на-льоту» | `InvoiceFormPage.tsx` |
| 4 кнопки оплати в POS | **Split «Оплатити (F2)»** ▾ методи | `ReceiptPanel.tsx` |
| Навігація лише мишею | **⌘K Command Palette** | новий `CommandPalette.tsx` |

## Додаток B: метрики успіху (як виміряємо, що стало краще)
- Кліків на прийом накладної: ~12 → ≤ 5.
- Час продажу клієнту з персональною ціною: ціль < 15с.
- Частка дій через ⌘K у досвідчених користувачів: > 30%.
- Sзприйнята швидкість: усі списки зі skeleton < 100ms до першого «каркасу».
- Нуль модалок на гарячому шляху каси.
