/**
 * У браузерній версії API працює на тому самому домені, тому базова адреса
 * порожня. Desktop-збірка й окреме локальне середовище можуть явно передати
 * VITE_API_URL без зміни клієнтського коду.
 */
export const API_BASE_URL = (import.meta.env.VITE_API_URL ?? '').trim().replace(/\/$/, '')

